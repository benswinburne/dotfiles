#!/usr/bin/env bash

echo "Installing Global Node Packages"

packages=(
  aws-cdk
  bundle-phobia-cli
  # cfn-lint — moved to Brewfile
  # cubejs-cli — niche, probably unused
  dockerfile-language-server-nodejs
  dynamodb-admin
  dynamodump
  fixjson
  forever
  gzip-size-cli
  hs
  htmlhint
  http-status-identifier-cli
  # intelephense — managed by mason.nvim
  node-gyp
  np
  npkill
  package-size
  release-it
  # remotedebug-ios-webkit-adapter — niche, probably unused
  # tc-string-parse — niche, probably unused
  uuid-cli
  bitly-client
  yalc
  # bash-language-server — managed by mason.nvim
)

npm install -g "${packages[@]}"
